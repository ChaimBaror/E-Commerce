# ממשק אדמין - הוראות התקנה

## הגדרת משתני סביבה

הוסף את האימיילים של האדמינים לקובץ `.env.local`:

```env
ADMIN_EMAILS=admin1@example.com,admin2@example.com
```

**חשוב:** הפרד בין אימיילים בפסיקים ללא רווחים.

## מבנה הממשק

### דפים זמינים:

- `/admin` - דשבורד ראשי עם סטטיסטיקות
- `/admin/products` - ניהול מוצרים (רשימה, הוספה, עריכה)
- `/admin/orders` - ניהול הזמנות (רשימה, פרטי הזמנה)
- `/admin/users` - ניהול משתמשים
- `/admin/analytics` - אנליטיקה ודוחות

### הגנה על הממשק

הממשק מוגן באמצעות:
1. **Middleware** - בודק אימות וזכויות גישה לפני כניסה ל-`/admin/*`
2. **Session Check** - כל דף בודק שהמשתמש מחובר
3. **Email Whitelist** - רק אימיילים ב-`ADMIN_EMAILS` יכולים לגשת

## שימוש

1. **התחברות:**
   - התחבר עם אימייל שנמצא ב-`ADMIN_EMAILS`
   - גש ל-`/admin`

2. **ניהול מוצרים:**
   - צפה ברשימת כל המוצרים
   - לחץ על "מוצר חדש" להוספה
   - לחץ על עריכה/מחיקה לניהול מוצר קיים

3. **ניהול הזמנות:**
   - צפה ברשימת כל ההזמנות
   - לחץ על הזמנה לצפייה בפרטים

## API Routes

### `/api/admin/dashboard`
- **GET** - מחזיר סטטיסטיקות לדשבורד
- דורש: אימות + הרשאות אדמין

### `/api/admin/stats`
- **GET** - מחזיר סטטיסטיקות מתקדמות
- דורש: אימות + הרשאות אדמין

## TODO - שיפורים עתידיים

- [ ] חיבור למסד נתונים אמיתי (Prisma/MongoDB)
- [ ] הוספת CRUD מלא למוצרים (יצירה, עדכון, מחיקה)
- [ ] הוספת ניהול הזמנות (שינוי סטטוס, עדכון)
- [ ] הוספת גרפים אמיתיים באנליטיקה
- [ ] הוספת מערכת תפקידים מתקדמת
- [ ] הוספת לוגים לפעולות אדמין
- [ ] הוספת חיפוש וסינון בטבלאות

## הערות

- כרגע הנתונים הם Mock Data
- יש לחבר למסד נתונים אמיתי לפרודקשן
- מומלץ להוסיף מערכת תפקידים מתקדמת במקום whitelist של אימיילים

